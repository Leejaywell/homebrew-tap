# Homebrew Tap for Welinker

Install the latest Welinker build from GitHub:

```bash
brew install Leejaywell/tap/welinker
```

Or tap first:

```bash
brew tap Leejaywell/tap
brew install welinker
```

Install the latest development build:

```bash
brew install Leejaywell/tap/welinker --HEAD
```
